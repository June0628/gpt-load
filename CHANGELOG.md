# 更新日志

## 未发布 (Unreleased)

### 新功能

- **上游配额信息过滤**：自动识别并过滤上游响应中额外嵌入的配额 JSON（含 `lastOne`、`quota` 等字段），避免客户端（如 Cherry Studio）因类型校验失败而报错。同时支持流式（SSE）和非流式两种响应模式。
- **配额不足飞书告警**：当上游渠道剩余请求次数低于阈值（500 次）时，自动通过飞书 Webhook 发送告警通知，附带分组、密钥和剩余次数信息，10 分钟冷却避免重复发送。
- **多代理地址轮询**：HTTP 代理地址（`PROXY_URL`）支持逗号分隔配置多个地址，通过原子计数器实现平滑轮询负载均衡，每个代理地址对应独立的 `http.Client` 实例。

### Bug 修复

- **修复配额 JSON 泄漏到客户端**：当非流式响应体中仅包含配额 JSON（无其他有效内容）时，`filterQuotaFromBody` 错误地返回了原始 body，导致配额 JSON 被原样转发给客户端。修复后返回空 body，阻断无效内容传递。
- **修复 SQLite 事务内查询死锁**：在 `UpdateGroup`、`DeleteGroup`、`CopyGroup` 中，将聚合分组引用检查、唯一名称生成等预检查逻辑移至事务开启之前，避免 SQLite 单连接模式下事务内查询导致死锁。

### 优化

- **流式响应处理重构**：`handleStreamingResponse` 从原始字节流读取改为按行解析 SSE 事件，支持检测和过滤自定义事件类型（`event:` 前缀），提升对非标准上游响应的兼容性。
- **代理池统一管理**：所有渠道（OpenAI、Anthropic、Gemini、OpenAI Responses）的密钥验证请求统一通过 `GetHTTPClient()` 方法获取客户端，确保密钥验证也享受多代理轮询能力。

### 国际化

- 更新代理地址配置项描述，补充多地址逗号分隔轮询的说明（中文/英文/日文）。
